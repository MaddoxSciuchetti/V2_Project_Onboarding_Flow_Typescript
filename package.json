{
  "name": "@acme/shared",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm -w @acme/shared run build",
    "dev:shared": "npm -w @acme/shared run dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
