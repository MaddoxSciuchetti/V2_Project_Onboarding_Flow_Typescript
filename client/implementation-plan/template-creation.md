Definition of done:

-When creating a new worker the user has the option to apply a template
-The template gets fetched from the current templates
-When the worker is being created, all of the template items are being converted into tasks

-
